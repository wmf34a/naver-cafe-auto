---
name: cafe-post
description: 네이버 카페에 글을 쓰고 고치고 발행한다. 공식 Cafe Open API를 쓰므로 브라우저 자동화나 로그인 세션이 필요 없다. 초안을 파일로 남겨 대화로 몇 번이든 고친 뒤, 승인했을 때만 올린다. "카페에 글 올려줘", "카페 글 써줘", "이 부분 고쳐서 다시 올려", "육진대 카페에 공지 하나 써줘" 같은 요청에 쓴다.
---

# cafe-post

네이버 카페 글쓰기. 초안은 **파일로 남는다** — 그래야 "3번째 문단 좀 줄여" 같은 말로 몇 번이든 고칠 수 있고, 대화가 끊겨도 살아남는다.

## 흐름

```
1. 초안 작성    → drafts/<날짜>-<슬러그>.md 에 쓴다
2. 사람이 읽는다 → "저 문단 빼", "제목 더 세게" → 파일을 고친다
3. 발행         → 사용자가 명시적으로 올리라고 할 때만
```

**2번을 건너뛰지 않는다.** 초안을 쓴 직후 발행하지 마라. 본문을 보여주고 승인을 받아라. 카페 글은 되돌리기 어렵고, 하루 200건 한도가 있다.

## 명령

```bash
S=.claude/skills/cafe-post/scripts/naver_cafe.py

python3 $S post drafts/글.md --cafe 육진대 --board 공지사항 --dry-run   # 먼저 이걸로 확인
python3 $S post drafts/글.md --cafe 육진대 --board 공지사항            # 멤버 공개
python3 $S post drafts/글.md --cafe 육진대 --board 공지사항 --public   # 전체 공개 + 검색 허용
```

`--dry-run` 을 먼저 돌려 변환된 HTML과 첨부 이미지 목록을 확인해라. 성공하면 `articleUrl` 이 나온다 — 그 링크를 사용자에게 준다.

## 초안 형식

첫 줄이 `# 제목` 이고 나머지가 본문이다.

```markdown
# 서울 근교 당일치기 BEST 3

## 첫째, 양평 두물머리

물안개가 **가장 예쁜 시간**은 이른 아침이다.
- 주차: 무료

![](사진1.jpg)
```

변환 결과는 **평문 + `<br>`** 이다. 카페 API가 다른 태그를 거절한다.

| 마크다운 | 카페에 올라가는 모양 |
|---|---|
| `## 소제목` | `■ 소제목` |
| `**굵게**` `*기울임*` | 표시 없이 글자만 |
| `- 항목` | `· 항목` |
| `[지도](https://a.b)` | `지도 (https://a.b)` |
| `![](사진.jpg)` | 첨부 이미지로 분리 |

굵게·기울임이 안 되니, 강조는 줄바꿈과 `■` 소제목으로 만들어라.

이미지 경로는 초안 파일 기준 상대경로다. **네이버가 첨부 이미지를 글 맨 끝에 붙인다** — 본문 중간 위치는 지정할 수 없다. 순서가 중요하면 사용자에게 미리 말해라.

## 사진 넣기 — 첨부 대신 <img>

`--image` 첨부는 **본문 맨 끝에 몰리고, 발행 후 에디터로 열면 깨진다.** 원하는 자리에 넣으려면 R2에 올려 URL로 쓴다.

```bash
python3 .claude/skills/cafe-post/scripts/upload_images.py <사진폴더> <접두어>
```

출력된 `![](https://...)` 줄을 초안의 원하는 문단 사이에 붙이면 된다. 변환기가 갈라서 처리한다.

| 초안에 쓴 것 | 결과 |
|---|---|
| `![](https://...)` | 본문 제자리에 `<img>` |
| `![](사진.jpg)` | 첨부로 분리 — 본문 맨 끝 |

R2 키에 내용 해시가 들어가서, 같은 사진은 다시 안 올라가고 사진을 바꾸면 URL이 바뀌어 캐시가 꼬이지 않는다.

사진이 많으면 **장면별로 묶어 한 장으로 합쳐라.** 네이버 에디터의 이미지 그룹(슬라이드)은 API로 못 만들지만, 콜라주로 같은 효과가 나고 용량도 크게 준다.

## 매번 사용자에게 받아야 하는 것

사진만으로는 알 수 없고 지어내면 안 되는 것들이다. 초안 쓰기 전에 물어라.

```
인원: 아빠 O명, 아이 O명
장소: 집결지 / 뒤풀이 식당
호명: 간식 O님, 인솔·사진 O님
```

**호명이 특히 중요하다.** 육진대 후기는 도와준 사람 이름을 불러주는 게 색깔이라, 그게 빠지면 남이 쓴 요약처럼 읽힌다.

## 카페 글 쓸 때

- 제목에 낚시를 넣지 마라. 카페는 블로그가 아니고, 회원들이 매번 본다
- 첫 두 줄에 결론을 넣어라. 카페 목록에서 미리보기로 보이는 부분이다
- 정보 출처가 있으면 링크로 밝혀라. 없는 수치를 지어내지 마라
- 길이는 화면 두세 번 스크롤 분량이면 충분하다

## 준비 (최초 1회)

1. [네이버 개발자센터](https://developers.naver.com/apps/#/register)에서 애플리케이션 등록
   - **API 권한관리 탭에서 "카페" 체크** — 안 하면 403 난다
   - Callback URL 에 `http://localhost:8080/callback` 등록
2. 환경변수 설정 (`~/.zshrc`)
   ```bash
   export NAVER_CLIENT_ID="..."
   export NAVER_CLIENT_SECRET="..."
   ```
3. 인증 — 브라우저가 열리고 동의하면 끝. refresh token이 `~/.naver-cafe/token.json` (0600)에 저장된다
   ```bash
   python3 .claude/skills/cafe-post/scripts/naver_cafe.py auth
   ```
4. `cafes.json` 에 카페와 게시판을 등록
   ```json
   { "육진대": { "clubid": "14447337", "boards": { "공지사항": 1, "장소추천": 2 } } }
   ```
   - `clubid`: 카페 관리 페이지 주소 `cafe.naver.com/ManageHome.nhn?clubid=14447337` 의 숫자
   - `menuid`: 게시판을 열었을 때 주소의 `menuid` 값

## 제약 — 실측으로 확인한 것

문서와 실제가 다르다. 아래는 2026-09-09 육진대 카페(clubid 31732268)에서 직접 올려보며 확인한 값이다.

| 항목 | 실제 |
|---|---|
| **연속 등록** | 약 **2분 간격** 필요. 더 빠르면 403 코드 999 |
| **인코딩 (텍스트만)** | **이중** — UTF-8 인코딩 후 MS949로 재인코딩 |
| **인코딩 (이미지 첨부)** | **단일** — UTF-8 퍼센트 인코딩. multipart는 규칙이 다르다 |
| 이미지 첨부(`--image`) | 본문 맨 끝에 몰리고, 발행 후 수정하면 깨진다(엑박) |
| `<img>` + 외부 URL | 문단 사이 배치 가능. 발행 후 수정해도 안 깨진다 — **이쪽을 기본으로 쓴다** |
| `<br>` | 통과 — 줄바꿈은 이걸로 |
| `<b>` `<i>` `<a>` `<blockquote>` `<hr>` | 통과. 서식도 살아난다 |
| `<a href>` | **홑따옴표 필수**. 쌍따옴표면 글 전체가 403 |
| **인용구 줄바꿈** | 인용구 안에서 `<br>` 은 글자로 보이고 개행문자는 무시된다. **줄마다 `<blockquote>` 를 따로** 열어야 한다 |
| `<` `>` `&` `'` | 통과 |
| `"` | **거절** → `&quot;` 로 치환해야 통과 (스크립트가 자동 처리) |

403 코드 999는 이 모든 경우에 똑같이 뜬다. 원인을 안 알려주므로, 실패하면 **먼저 2분 기다렸다 재시도**해라. 대부분 연속 등록 제한이다.

## 문서에서 확인한 것

- **글쓰기 200건/일**, 네이버 계정당
- 글쓰기 권한이 있는 등급으로 **그 카페에 가입돼 있어야** 한다
- **상품게시판에는 못 쓴다**
- 카페 금칙어·스팸 자동차단에 걸리면 스팸함으로 간다 (에러 `AP005`)

## 문제 생기면

| 증상 | 원인 |
|---|---|
| 403 코드 999 | 십중팔구 연속 등록 제한. 2분 뒤 재시도 |
| 그래도 403 | 본문에 `<br>` 외 태그가 섞였는지 확인 |
| 403 + 권한 없음 | 개발자센터 API 권한관리에서 "카페" 미체크 |
| 403 `AP003` | 그 게시판에 쓸 등급이 아니다 |
| 403 `AP004` | 글쓰기가 막힌 게시판이다 |
| 토큰 갱신 실패 | refresh token 만료. `auth` 다시 |

전송 방식에 따라 인코딩이 갈리는 게 제일 함정이다. 반대로 쓰면 양쪽 다 깨진다. 문서 Java 예제도 form 쪽은 이중, multipart 쪽은 단일로 서로 다르게 써 놨는데 그게 오타가 아니라 실제 규칙이었다.

**HTTP 200을 성공으로 믿지 마라.** 200은 접수됐다는 뜻이고, 한글이 깨진 채로도 200이 온다. 인코딩 관련해 뭔가 바꿨으면 카페에 올라간 글자를 눈으로 봐야 한다.

## 자체 검증

```bash
cd .claude/skills/cafe-post/scripts && python3 test_naver_cafe.py
```

파싱·인코딩·multipart 조립을 네트워크 없이 확인한다. 스크립트를 고쳤으면 이걸 돌려라.
